define([
    'jquery',
    'Magento_Customer/js/customer-data'
], function ($, customerData) {
    'use strict';

    return function (config, element) {
        /*$(document).on('click', '.towishlist', function () {
            console.log('Wishlist button clicked+++++pppppp_');
            customerData.reload(['wishlist'], true);
        });*/
    };
});
