<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="StorefrontCheckThatQuoteItemDownloadLinksUpdatedAfterStagingUpdateTest">
        <annotations>
            <features value="DownloadableStaging"/>
            <stories value="Staging updates"/>
            <title value="Check that downloadable product links in cart will be updated after scheduled update"/>
            <description value="Check that links of downloadable product in cart will be updated with new links ids from scheduled update"/>
            <severity value="AVERAGE" />
            <testCaseId value="AC-7938"/>
            <useCaseId value="ACP2E-1596"/>
            <group value="staging"/>
            <group value="downloadableStaging"/>
        </annotations>

        <before>
            <!-- Add downloadable domains -->
            <magentoCLI command="downloadable:domains:add example.com static.magento.com" stepKey="addDownloadableDomain"/>

            <!-- Create Downloadable Product -->
            <createData entity="ApiDownloadableProduct" stepKey="createDownloadableProduct"/>
            <createData entity="downloadableLink1" stepKey="addDownloadableLink1">
                <requiredEntity createDataKey="createDownloadableProduct"/>
            </createData>
            <createData entity="downloadableLink2" stepKey="addDownloadableLink2">
                <requiredEntity createDataKey="createDownloadableProduct"/>
            </createData>

            <createData entity="Simple_US_Customer" stepKey="createCustomer"/>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
        </before>

        <after>
            <actionGroup ref="StorefrontCustomerLogoutActionGroup" stepKey="customerLogout"/>
            <actionGroup ref="StorefrontCustomerLogoutActionGroup" stepKey="logoutCustomer" />
            <deleteData createDataKey="createCustomer" stepKey="deleteCustomer"/>
            <deleteData createDataKey="createDownloadableProduct" stepKey="deleteProduct"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>

        <actionGroup ref="LoginToStorefrontActionGroup" stepKey="loginAsCustomer">
            <argument name="Customer" value="$createCustomer$"/>
        </actionGroup>
        <actionGroup ref="OpenStoreFrontProductPageActionGroup" stepKey="goToProductPageFirst">
            <argument name="productUrlKey" value="$createDownloadableProduct.custom_attributes[url_key]$"/>
        </actionGroup>
        <actionGroup ref="StorefrontAddProductToCartActionGroup" stepKey="addProductToCartFirst">
            <argument name="product" value="$createDownloadableProduct$" />
            <argument name="productCount" value="1" />
        </actionGroup>
        <actionGroup ref="StorefrontCartPageOpenActionGroup" stepKey="goToCheckoutCartPage"/>
        <actionGroup ref="AssertStorefrontCheckoutCartItemsActionGroup" stepKey="assertProductItemInCheckOutCart">
            <argument name="productName" value="$createDownloadableProduct.name$"/>
            <argument name="productSku" value="$createDownloadableProduct.sku$"/>
            <argument name="productPrice" value="$createDownloadableProduct.price$"/>
            <argument name="subtotal" value="$createDownloadableProduct.price$" />
            <argument name="qty" value="1"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeLinksInCart">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="Links"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeProductOptionLink1">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="{{downloadableLink1.title}}"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeProductOptionLink2">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="{{downloadableLink2.title}}"/>
        </actionGroup>


        <amOnPage url="{{AdminProductEditPage.url($createDownloadableProduct.id$)}}" stepKey="openAdminEditProductPage"/>
        <waitForPageLoad stepKey="waitForEditPageLoad"/>

        <executeJS function="
                var today = new Date();
                var sec = today.getSeconds();
                return sec > 50 ? (61-sec) : 0;" stepKey="needWait"/>
        <wait time="$needWait" stepKey="conditionalWait"/>

        <generateDate date="+1 minute" format="m/d/Y g:i A" stepKey="generateStartDate"/>
        <executeJS function="var today = new Date();
                var sec = today.getSeconds();
                return 60 - sec;" stepKey="numSecondsLeft"/>
        <executeJS function="return Math.floor(new Date() / 1000);" stepKey="getStartingTime"/>

        <generateDate date="+1 week" format="m/d/Y g:i A" stepKey="generateEndDate"/>
        <actionGroup ref="FillScheduleUpdateActionGroup" stepKey="createNewStagingUpdate">
            <argument name="staging" value="S5UpdateNameStaging"/>
            <argument name="timeStart" value="{$generateStartDate}"/>
            <argument name="timeEnd" value="{$generateEndDate}"/>
            <argument name="productPrice" value="100"/>
        </actionGroup>

        <executeJS function="return Math.floor(new Date() / 1000);" stepKey="getEndingTime"/>
        <executeJS function="var timePassed = {$getEndingTime} - {$getStartingTime};
                var timeLeft = {$numSecondsLeft} - timePassed;
                return timeLeft > 0 ? timeLeft : 0;" stepKey="getTimeToWait"/>
        <wait time="$getTimeToWait" stepKey="conditionalWait2"/>

        <actionGroup ref="ApplyUpdatedRuleStagingByCron" stepKey="runCronTwiceToApplyStagingUpdate"/>
        <actionGroup ref="CliIndexerReindexActionGroup" stepKey="reindex">
            <argument name="indices" value=""/>
        </actionGroup>

        <actionGroup ref="StorefrontCartPageOpenActionGroup" stepKey="goToCheckoutCartPageAfterUpdateFirst"/>
        <actionGroup ref="AssertStorefrontCheckoutCartItemsActionGroup" stepKey="assertProductItemInCheckOutCartAfterUpdateFirst">
            <argument name="productName" value="$createDownloadableProduct.name$"/>
            <argument name="productSku" value="$createDownloadableProduct.sku$"/>
            <argument name="productPrice" value="100"/>
            <argument name="subtotal" value="100" />
            <argument name="qty" value="1"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeLinksInCartAfterUpdateFirst">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="Links"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeProductOptionLink1AfterUpdateFirst">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="{{downloadableLink1.title}}"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeProductOptionLink2AfterUpdateFirst">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="{{downloadableLink2.title}}"/>
        </actionGroup>

        <actionGroup ref="OpenStoreFrontProductPageActionGroup" stepKey="goToProductPageSecond">
            <argument name="productUrlKey" value="$createDownloadableProduct.custom_attributes[url_key]$"/>
        </actionGroup>
        <actionGroup ref="StorefrontAddProductToCartActionGroup" stepKey="addProductToCartSecond">
            <argument name="product" value="$createDownloadableProduct$" />
            <argument name="productCount" value="2" />
        </actionGroup>

        <actionGroup ref="StorefrontCartPageOpenActionGroup" stepKey="goToCheckoutCartPageAfterUpdateSecond"/>
        <actionGroup ref="AssertStorefrontCheckoutCartItemsActionGroup" stepKey="assertProductItemInCheckOutCartAfterUpdateSecond">
            <argument name="productName" value="$createDownloadableProduct.name$"/>
            <argument name="productSku" value="$createDownloadableProduct.sku$"/>
            <argument name="productPrice" value="100"/>
            <argument name="subtotal" value="200" />
            <argument name="qty" value="2"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeLinksInCartAfterUpdateSecond">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="Links"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeProductOptionLink1AfterUpdateSecond">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="{{downloadableLink1.title}}"/>
        </actionGroup>
        <actionGroup ref="AssertStorefrontElementVisibleActionGroup" stepKey="seeProductOptionLink2AfterUpdateSecond">
            <argument name="selector" value="{{CheckoutCartProductSection.productOptionLabel}}"/>
            <argument name="userInput" value="{{downloadableLink2.title}}"/>
        </actionGroup>
    </test>
</tests>
