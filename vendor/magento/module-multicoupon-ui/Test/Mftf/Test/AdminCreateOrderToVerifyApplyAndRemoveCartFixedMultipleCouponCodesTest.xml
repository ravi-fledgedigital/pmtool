<?xml version="1.0" encoding="UTF-8"?>
<!--
/************************************************************************
 *
 * Copyright 2024 Adobe
 * All Rights Reserved.
 *
 * NOTICE: All information contained herein is, and remains
 * the property of Adobe and its suppliers, if any. The intellectual
 * and technical concepts contained herein are proprietary to Adobe
 * and its suppliers and are protected by all applicable intellectual
 * property laws, including trade secret and copyright laws.
 * Dissemination of this information or reproduction of this material
 * is strictly forbidden unless prior written permission is obtained
 * from Adobe.
 * ************************************************************************
 */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminCreateOrderToVerifyApplyAndRemoveCartFixedMultipleCouponCodesTest">
        <annotations>
            <stories value="Create Order with multiple coupon codes"/>
            <title value="Create an order to verify the appliance and removal of multiple cart-fixed discount coupon codes"/>
            <description value="Create an order to verify the appliance and removal of multiple cart-fixed discount coupon codes"/>
            <severity value="AVERAGE"/>
            <testCaseId value="LYNX-367"/>
            <skip>
                <issueId value="LYNX-372"/>
            </skip>
        </annotations>
        <before>
            <magentoCLI
                command="config:set {{EnablePaymentBankTransferConfigData.path}} {{EnablePaymentBankTransferConfigData.value}}"
                stepKey="enableBankTransferPayment"/>
            <magentoCLI command="config:set {{EnableFlatRateConfigData.path}} {{EnableFlatRateConfigData.value}}"
                        stepKey="enableFlatRate"/>
            <magentoCLI command="config:set sales/multicoupon/maximum_number_of_coupons_per_order 3" stepKey="EnablingMulticoupon"/>
            <actionGroup ref="AdminLoginActionGroup" stepKey="LoginAsAdmin"/>
            <createData entity="Simple_US_Customer" stepKey="createCustomer"/>
            <createData entity="SimpleProduct2" stepKey="createSimpleProduct">
                <field key="price">100</field>
            </createData>
            <createData entity="SalesRuleSpecificCouponWithFixedDiscount" stepKey="createCartPriceRule"/>
            <createData entity="SimpleSalesRuleCoupon" stepKey="createCouponForCartPriceRule">
                <requiredEntity createDataKey="createCartPriceRule"/>
            </createData>
            <createData entity="SalesRuleSpecificCouponWithFixedDiscount" stepKey="createCartPriceRule2"/>
            <createData entity="SimpleSalesRuleCoupon" stepKey="createCouponForCartPriceRule2">
                <requiredEntity createDataKey="createCartPriceRule2"/>
            </createData>
            <createData entity="SalesRuleSpecificCouponWithFixedDiscount" stepKey="createCartPriceRule3"/>
            <createData entity="SimpleSalesRuleCoupon" stepKey="createCouponForCartPriceRule3">
                <requiredEntity createDataKey="createCartPriceRule3"/>
            </createData>
        </before>
        <after>
            <magentoCLI command="config:set sales/multicoupon/maximum_number_of_coupons_per_order 1" stepKey="DisablingMulticoupon"/>
            <magentoCLI
                command="config:set {{DisablePaymentBankTransferConfigData.path}} {{DisablePaymentBankTransferConfigData.value}}"
                stepKey="disableBankTransferPayment"/>
            <deleteData createDataKey="createCartPriceRule" stepKey="deleteCartPriceRule"/>
            <deleteData createDataKey="createCartPriceRule2" stepKey="deleteCartPriceRule2"/>
            <deleteData createDataKey="createCartPriceRule3" stepKey="deleteCartPriceRule3"/>
            <deleteData createDataKey="createCustomer" stepKey="deleteCustomer"/>
            <deleteData createDataKey="createSimpleProduct" stepKey="deleteSimpleProduct"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>

        <!-- Create order for registered customer -->
        <actionGroup ref="AdminNavigateToNewOrderPageExistingCustomerActionGroup" stepKey="goToCreateOrderPage">
            <argument name="customer" value="$$createCustomer$$"/>
        </actionGroup>

        <!-- Add Simple product to order -->
        <actionGroup ref="AddSimpleProductToOrderActionGroup" stepKey="addProductToOrder">
            <argument name="product" value="$$createSimpleProduct$$"/>
        </actionGroup>

        <!-- Apply coupon codes to the order -->
        <actionGroup ref="AdminApplyCouponToOrderActionGroup" stepKey="applyCoupon">
            <argument name="couponCode" value="$$createCouponForCartPriceRule.code$$"/>
        </actionGroup>
        <see selector="{{OrdersGridSection.discount($$createSimpleProduct.name$$)}}" userInput="-$10.00" stepKey="seeDiscountAmountAfterApplyCoupon"/>
        <actionGroup ref="AdminApplyCouponToOrderActionGroup" stepKey="applyCoupon2">
            <argument name="couponCode" value="$$createCouponForCartPriceRule2.code$$"/>
        </actionGroup>
        <see selector="{{OrdersGridSection.discount($$createSimpleProduct.name$$)}}" userInput="-$20.00" stepKey="seeDiscountAmountAfterApplyCoupon2"/>
        <actionGroup ref="AdminApplyCouponToOrderActionGroup" stepKey="applyCoupon3">
            <argument name="couponCode" value="$$createCouponForCartPriceRule3.code$$"/>
        </actionGroup>
        <see selector="{{OrdersGridSection.discount($$createSimpleProduct.name$$)}}" userInput="-$30.00" stepKey="seeDiscountAmountAfterApplyCoupon3"/>

        <!-- Remove coupon code to the order -->
        <actionGroup ref="AdminRemoveSpecificCouponFromOrderActionGroup" stepKey="removeCoupon2">
            <argument name="couponCode" value="$$createCouponForCartPriceRule2.code$$"/>
        </actionGroup>
        <see selector="{{OrdersGridSection.discount($$createSimpleProduct.name$$)}}" userInput="-$20.00" stepKey="seeDiscountAmountAfterRemoveCoupon2"/>

        <!-- Select FlatRate shipping method -->
        <actionGroup ref="AdminSelectFlatRateShippingMethodActionGroup" stepKey="selectFlatRateShippingMethod"/>

        <!-- Select bank Transfer payment method and submit the order -->
        <waitForElementVisible selector="{{AdminOrderFormPaymentSection.paymentBlock}}"
                               stepKey="waitForPaymentOptions"/>
        <conditionalClick selector="{{AdminOrderFormPaymentSection.bankTransferOption}}"
                          dependentSelector="{{AdminOrderFormPaymentSection.bankTransferOption}}" visible="true"
                          stepKey="checkBankTransferOption"/>
        <actionGroup ref="AdminSubmitOrderActionGroup" stepKey="submitOrder"/>

        <!-- Verify order information -->
        <grabTextFrom selector="|Order # (\d+)|" stepKey="orderNumber"/>
        <actionGroup ref="AdminOrdersPageOpenActionGroup" stepKey="goToOrdersPage"/>
        <actionGroup ref="FilterOrderGridByIdActionGroup" stepKey="filterOrdersGridById">
            <argument name="orderId" value="{$orderNumber}"/>
        </actionGroup>
        <click selector="{{AdminDataGridTableSection.firstRow}}" stepKey="clickCreatedOrderInGrid"/>
        <waitForPageLoad after="clickCreatedOrderInGrid" stepKey="waitForOrderViewPageOpened"/>

        <!-- Verify the discounts were applied -->
        <see selector="{{AdminOrderTotalSection.discount}}" userInput="-$20.00" stepKey="seeDiscountAmount"/>
    </test>
</tests>
