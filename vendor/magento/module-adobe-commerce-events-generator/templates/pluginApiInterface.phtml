<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/** @var \Magento\AdobeCommerceOutOfProcessExtensibility\Model\Generator\ModuleBlock $block */
/** @var string $namespace */
/** @var string $name */
/** @var string $interface */
/** @var string $interfaceShort */
/** @var array $methods */
?>
<?='<?php'; ?>

# Generated code. DO NOT EDIT!

/************************************************************************
 *
 * ADOBE CONFIDENTIAL
 * ___________________
 *
 * Copyright 2024 Adobe
 * All Rights Reserved.
 *
 * NOTICE: All information contained herein is, and remains
 * the property of Adobe and its suppliers, if any. The intellectual
 * and technical concepts contained herein are proprietary to Adobe
 * and its suppliers and are private by all applicable intellectual
 * property laws, including trade secret and copyright laws.
 * Dissemination of this information or reproduction of this material
 * is strictly forbidden unless prior written permission is obtained
 * from Adobe.
 * ************************************************************************
 */
declare(strict_types=1);

namespace <?=$namespace ?>;

use Magento\AdobeCommerceEventsClient\Event\Converter\EventDataConverter;
use Magento\AdobeCommerceEventsClient\Event\EventList;
use Magento\AdobeCommerceEventsClient\Event\EventSubscriberInterface;
use Magento\AdobeCommerceEventsClient\Event\Processor\EventProcessorInterface;
use Magento\Framework\Webapi\ServiceOutputProcessor;
use <?=$interface ?>;
use Psr\Log\LoggerInterface;

/**
 * Auto generated plugin for <?=$interface . PHP_EOL ?>
 */
class <?=$name . PHP_EOL ?>
{
    private const SERVICE = '<?=$interface ?>';

    /**
     * @var EventList
     */
    private $eventList;

    /**
     * @var EventDataConverter
     */
    private $eventDataConverter;

    /**
     * @var LoggerInterface
     */
    private $logger;

    /**
     * @var EventProcessorInterface
     */
    private $eventProcessor;

    /**
     * @var ServiceOutputProcessor
     */
    private $serviceOutputProcessor;

    /**
     * @param EventList $eventList
     * @param EventDataConverter $eventDataConverter
     * @param LoggerInterface $logger
     * @param EventProcessorInterface $eventProcessor
     * @param ServiceOutputProcessor $serviceOutputProcessor
     */
    public function __construct(
        EventList $eventList,
        EventDataConverter $eventDataConverter,
        LoggerInterface $logger,
        EventProcessorInterface $eventProcessor,
        ServiceOutputProcessor $serviceOutputProcessor
    ) {
        $this->eventList = $eventList;
        $this->eventDataConverter = $eventDataConverter;
        $this->logger = $logger;
        $this->eventProcessor = $eventProcessor;
        $this->serviceOutputProcessor = $serviceOutputProcessor;
    }

<?php foreach ($methods as $method): ?>

    /**
     * Intercepts <?=$method['name'] ?> method and invokes after subscriptions if they are available
     *
     * @param <?=$interfaceShort ?> $subject
     * @param mixed $result
<?php foreach ($method['params'] as $param): ?>
     * @param $<?=$param['name'] . PHP_EOL; ?>
<?php endforeach; ?>
     * @return mixed
     */
    public function after<?=$method['name'] ?>(<?=$interfaceShort ?> $subject, $result<?=$block->renderParametersForMethod($method); ?>)
    {
        try {
            $eventCode = EventSubscriberInterface::EVENT_PREFIX_COMMERCE .
                EventSubscriberInterface::EVENT_TYPE_PLUGIN . '.<?=$method['eventCode'] ?>';

            if (!$this->eventList->isEventEnabled($eventCode)) {
                return $result;
            }

            if (is_bool($result)) {
                if ($result) {
                    $data = [
<?php foreach ($method['params'] as $param): ?>
                        '<?=$param['name'] ?>' => $<?=$param['name'] ?>,
<?php endforeach; ?>
                    ];

                    $this->eventProcessor->processEvent(
                        $eventCode,
                        $this->eventDataConverter->convert($data)
                    );
                }
            } else {
                $outputValue = $this->serviceOutputProcessor->process($result, self::SERVICE, '<?=$method['nameLower'] ?>');
                $this->eventProcessor->processEvent(
                    $eventCode,
                    is_array($outputValue) ? $outputValue : [$outputValue]
                );
            }
        } catch (\Throwable $e) {
            $this->logger->error(
                sprintf(
                    'Failed to save event data. Event code: "%s", Error: "%s"',
                    $eventCode,
                    $e->getMessage()
                ),
                ['exception' => $e]
            );
        }

        return $result;
    }
<?php endforeach; ?>
}
