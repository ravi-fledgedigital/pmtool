## Release notes

*Magento_BundleProductOverrideDataExporter* module

This is a temporary module to fix a bug in the catalog-staging module.
This module allows bundle tables to use the parent_product column that is linked to the row_id in the product table.